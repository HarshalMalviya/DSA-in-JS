# input

```js
const fs = require("fs");

const input = fs.readFileSync(0, "utf8").trim();
```

------------------------------------------------------------------------------------------
# pattern printing

1. outer loop with row declaration
2. connect outer loop with inner somehow
3. inner loop with updated row
4. return between inner and outer
5. observe the symmetry or pattern